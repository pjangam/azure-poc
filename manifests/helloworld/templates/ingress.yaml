apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress-nginx
spec:
  rules:
    - host: 51.141.165.251.xip.io
      http:
        paths:
          - path: /
            backend:
              serviceName: pjangamazurepoc
              servicePort: 80
